argus_module(NAME guard
    SOURCES
        guard-action.cc
        guard-assessment.cc
        guard-belief.cc
        guard-dialogue.cc
        guard-policy.cc
        guard-repository.cc
        guard-risk.cc
        guard-schema.cc
        guard-service.cc
    DEPENDS
        argus_common
        argus::sqlite
        argus::sdk-identity
        argus::sdk-notification
        argus::sdk-camera-actions
        argus::llm-client
        argus::vlm-client
    INCLUDES
        ${CMAKE_CURRENT_SOURCE_DIR})
