# argus-deploy tunnel-client configuration (Fase 5). Copy to
# config.tunnel.toml (gitignored) next to this file; the compose file
# bind-mounts it as argus-tunnel-client's config.toml.

[tunnel]
secret = ""

# Home-host client knobs.
reconnect_wait_ms = 2000
max_reconnects = 60
ping_interval_seconds = 30

# Link knobs shared with the relay.
stream_idle_seconds = 300
max_streams = 256
socket_snd_buf = 0

[server]
relay_host = "127.0.0.1"
relay_port = 7101
gateway_host = "127.0.0.1"
gateway_port = 7034

health_host = "127.0.0.1"
health_port = 7104

[drogon.app]
number_of_threads = 0
enable_session = false
document_root = "./"
use_sendfile = true
use_gzip = false
use_brotli = false
static_files_cache_time = 5
idle_connection_timeout = 60
enable_server_header = true
enable_date_header = true
keepalive_requests = 0
pipelining_requests = 0
client_max_body_size = "1M"
client_max_memory_body_size = "1M"
reuse_port = false
handle_sig_term = true
relaunch_on_error = false
